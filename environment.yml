name: cot-analysis
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    # Inference serving
    - vllm
    # Data collection
    - openai>=1.50.0
    - httpx>=0.27.0
    # Data processing
    - pandas>=2.2.0
    - pyarrow>=15.0.0
    - numpy>=1.26.0
    # Dataset loading
    - datasets>=3.0.0
    # Utilities
    - tqdm>=4.66.0
    - python-dotenv>=1.0.0
    - pydantic>=2.6.0
    - jsonlines>=4.0.0
